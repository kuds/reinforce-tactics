# MaskablePPO baseline training.
#
# Overrides on top of TrainingConfig() defaults — see
# reinforcetactics/rl/config.py for the full schema and defaults.
# Only fields that differ from defaults need to appear here.
#
# Run: python scripts/train/train_feudal_rl.py --config configs/maskable_ppo.yaml
algorithm: maskable_ppo
total_timesteps: 10000000

env:
  max_steps: 500
